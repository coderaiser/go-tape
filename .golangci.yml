version: "2"
linters:
  enable:
    - staticcheck
    - unused
    - gocritic
    - misspell
  disable:
    - errcheck
  exclusions:
    paths:
      - node_modules
formatters:
  enable:
    - gofmt
    - goimports
