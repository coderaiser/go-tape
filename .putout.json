{
    "rules": {
        "goreleaser": "off"
    },
    "match": {
        ".goreleaser.yaml": {
            "goreleaser": "on"
        }
    },
    "plugins": ["goreleaser"],
    "ignore": [
        "invalid.json"
    ]
}
